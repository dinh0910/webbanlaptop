@model IEnumerable<webbanlaptop.Models.ThongTinSanPham>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table table-hover" id="myTable">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SanPhams)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ThongTins)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SanPhams.SanPhamID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ThongTins.ThongTinID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ThongTinSanPhamID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ThongTinSanPhamID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ThongTinSanPhamID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
