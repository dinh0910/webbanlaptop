@model IEnumerable<webbanlaptop.Models.TenThongSo>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Ten)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LoaiThongSos)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Ten)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LoaiThongSos.LoaiThongSoID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TenThongSoID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TenThongSoID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TenThongSoID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
